- scrolling is too fast, make it slower

- implement layers of masks (like in Lightroom). each layer is a separate mask.
  - moving layers
  - renaming layers

- implement intensity of masks (0 to 100%)